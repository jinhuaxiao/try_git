require(["jquery", "jquery.alpha", "jquery.beta", "jquery.kama"], function($) {
    //the jquery.alpha.js and jquery.beta.js plugins have been loaded.
    $(function() {
        $('body').alpha().beta().kama();
    });
});
